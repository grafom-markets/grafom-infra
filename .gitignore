# Environment files with real credentials
.env
ec2/.env

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.swp
*.swo

# SSH keys (should never be committed)
*.pem
*.key

# Terraform state and plans (may contain credentials)
*.tfstate
*.tfstate.backup
*.tfstate.*.backup
tfplan
.terraform/
